@if(country is null)
{
    <Loading/>
}
else
{
    <MudDialog>
        <DialogContent>
            <FormWithName @ref="countryForm" Label="País:" Model="country" OnValidSubmit="EditAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}