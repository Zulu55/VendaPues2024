@if (bank is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <FormWithName @ref="bankForm" Label="Banco: " Model="bank" OnValidSubmit="EditAsync" ReturnAction="Return"/>
        </DialogContent>
    </MudDialog>
}